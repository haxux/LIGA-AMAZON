change: fase-2-modelo-de-datos
title: "Fase 2 — Modelo de datos: migraciones, modelos y relaciones"
artifact_store: openspec
last_updated: 2026-08-07

session_settings:
  execution_mode: interactive
  delivery_strategy: auto-chain
  chain_strategy: stacked-to-main

phases:
  exploration:
    status: done
    artifact: openspec/changes/fase-2-modelo-de-datos/exploration.md
  proposal:
    status: done
    artifact: openspec/changes/fase-2-modelo-de-datos/proposal.md
  spec:
    status: done
    artifacts:
      - openspec/changes/fase-2-modelo-de-datos/specs/league-data-model/spec.md
      - openspec/changes/fase-2-modelo-de-datos/specs/public-file-storage/spec.md
  design:
    status: done
    artifact: openspec/changes/fase-2-modelo-de-datos/design.md
    note: "Future-Proofing Check section added after user feedback on 2026-08-07 (see memory: feedback-schema-extensibility)"
  tasks:
    status: done
    artifact: openspec/changes/fase-2-modelo-de-datos/tasks.md
    task_count: 28
    work_units:
      - { id: 1, name: migrations, branch: fase-2/1-migrations }
      - { id: 2, name: models, branch: fase-2/2-models }
      - { id: 3, name: factories, branch: fase-2/3-factories }
      - { id: 4, name: seeder, branch: fase-2/4-seeder }
      - { id: 5, name: storage-and-verify, branch: fase-2/5-storage-and-verify }
  apply:
    status: done
    artifact: openspec/changes/fase-2-modelo-de-datos/apply-progress.md
    note: "All 28/28 tasks complete across 5 stacked branches. Working tree clean on fase-2/5-storage-and-verify, all completed work committed."
    checkpoint:
      head_branch: fase-2/5-storage-and-verify
      done_units:
        - { id: 1, name: migrations, branch: fase-2/1-migrations, commit: 08993d9, tests: "12/12 pass" }
        - { id: 2, name: models, branch: fase-2/2-models, commit: c2e1791, tests: "5/5 pass" }
        - { id: 3, name: factories, branch: fase-2/3-factories, commit: 199b44a, tests: "7/7 pass" }
        - { id: 4, name: seeder, branch: fase-2/4-seeder, commit: dd5af2b, tests: "5/5 pass" }
        - { id: 5, name: storage-and-verify, branch: fase-2/5-storage-and-verify, tests: "31/31 pass (full suite, single invocation)" }
      not_started_units: []
      known_issue_fixed_this_run:
        summary: "php artisan test was silently running against the real dev MySQL DB instead of SQLite :memory: — docker-compose's container-level env vars outrank phpunit.xml overrides in Laravel's env resolution."
        fixed_in: "tests/TestCase.php (branch fase-2/1-migrations) — no .env*/docker-compose.yml touched"
        flagged_for: "user should skim this fix, affects all future test runs in this repo, not just Fase 2"
      known_issue_fixed_unit_5:
        summary: "storage/app/public/.gitignore's `*` pattern silently blocked git from ever tracking a new crests/ subdirectory (nested .gitignore alone was invisible to git without a parent negation)."
        fixed_in: "storage/app/public/.gitignore — added `!crests/` (branch fase-2/5-storage-and-verify)"
      storage_mechanism_resolved: "symlink (php artisan storage:link) — probe file served 200 OK over HTTP on first attempt. nginx alias fallback (docker/nginx/default.conf) was NOT needed and file remains untouched."
      full_suite_run_status: "Run as one single `php artisan test` invocation across all 7 test files together — 31/31 passed, 52 assertions. Task 6.1 complete."
      resume_instructions: "Apply phase complete. Next: sdd-verify. All 5 branches (fase-2/1-migrations through fase-2/5-storage-and-verify) remain unmerged for user review per chain_strategy: stacked-to-main — do not merge into master without explicit user instruction."
  verify:
    status: pending
  archive:
    status: pending

decisions_locked:
  - "Game.home_score/away_score nullable = not played, no status enum"
  - "Team.crest_path = real uploaded image file; storage groundwork in this change, Filament UI itself is Fase 3"
  - "Storage serving: storage:link attempt, nginx alias (docker/nginx/default.conf) committed as fallback; apply records which mechanism actually serves"
  - "FK strategy: cascadeOnDelete for single-owner chains, restrictOnDelete on Game.home_team_id/away_team_id"
  - "PHPUnit only for strict_tdd tests (Pest not installed)"
  - "home_team_id <> away_team_id enforced via Game::booted() Eloquent guard (ValidationException), not raw SQL CHECK"
  - "No is_current/active-season flag on Season (explicitly declined by user)"
  - "Seed: 1 season, 10 teams (Fase-5 mockup division A names), 180 players, 18 matchdays, 90 games, MD 1-10 scored / 11-18 null, crest_path null in seed data"
  - "DatabaseSeeder uses WithoutModelEvents (pre-existing) — seeder guarantees distinct teams by construction (circle-method round-robin), does not rely on the Game guard"

out_of_scope_this_change:
  - "Division/sub-league support within a Season — deferred, additive-migration-friendly per Future-Proofing Check"
  - "News/Article entity — deferred, unrelated to this schema"
  - "Player match-level event stats (goals/assists/cards) — deferred, additive table when built"
  - "Filament Resources (admin CRUD for these models) — Fase 3"
  - "StandingsService (computing the table) — Fase 4"
  - "Public Blade views — Fase 5"

next_action_if_resuming: >
  Apply phase is done (28/28 tasks, 5/5 branches). Next step is sdd-verify
  against the fase-2/5-storage-and-verify branch (tip of the stack). Do not
  re-ask the user execution_mode/delivery_strategy/chain_strategy — they're
  cached above. Do not merge any of the 5 branches into master without
  explicit user instruction — they are deliberately left unmerged for review.
